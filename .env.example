# Public site metadata (safe to expose to the browser)
NEXT_PUBLIC_SITE_URL=https://www.dfsa.example
NEXT_PUBLIC_SITE_NAME="DFSA"

# Analytics / third-party keys (fill in per environment, never commit real values)
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_GTM_ID=

# Server-only secrets (never prefixed with NEXT_PUBLIC_)
# CMS_API_TOKEN=
